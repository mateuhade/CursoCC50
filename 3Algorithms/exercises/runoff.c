#include <cs50.h>
#include <stdio.h>
#include <string.h>

MAX_CANDIDATES =

typedef struct {
    string name;
    int votes[];
} candidate;

int main(int argc, string argv[]) {
    candidate candidates[argc];
    candidates[0].votes[0] = 0;
    candidates[0].votes[1] = 0;

    int voters = get_int("Number of voters: ");



}


int vote(int voters, candidate candidates[]) {

    string currentVote;

    for (int i = 0; i < )
}
